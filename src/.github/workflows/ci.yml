name: CI/CD Pipeline
on:
  push:
    branches: ["main", "dev"]

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      # Test du code (Consigne 2)
      - name: 🧪 Installation et Tests
        run: |
          sudo npm install -g htmlhint
          htmlhint src/index.html

      # (On ajoutera le déploiement ici à l'étape 5)
